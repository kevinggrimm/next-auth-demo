import React from "react";

// TODO - get props from the session? may not be the right place
export default function DashboardNew(props) {
    return (
        <p>This is the user dashboard</p>
    )
}